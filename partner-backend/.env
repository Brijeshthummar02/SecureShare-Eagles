# Partner Backend Environment Variables

# Server Configuration
PORT=3001
NODE_ENV=development

# Partner Credentials (from bank registration)
PARTNER_ID=your_partner_id_here
API_TOKEN=your_api_token_here

# Bank API Configuration
BANK_API_URL=http://localhost:5000/api/v1

# RSA Key Pair for Encryption/Decryption (Partner's own keys)
PRIVATE_KEY_PATH=./keys/private_key.pem
PUBLIC_KEY_PATH=./keys/public_key.pem

# Or paste keys directly (base64 encoded)
# PRIVATE_KEY_BASE64=
# PUBLIC_KEY_BASE64=
